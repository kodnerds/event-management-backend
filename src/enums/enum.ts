export enum PaymentStatus {
  PENDING = 'PENDING',
  SUCCESS = 'SUCCESS',
  FAILED = 'FAILED'
}

export enum RsvpStatus {
  REGISTERED = 'REGISTERED',
  CANCELLED = 'CANCELLED',
  PENDING = 'PENDING'
}
